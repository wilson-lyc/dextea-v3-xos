# 对象存储中台 API 文档

本系统提供统一的文件上传接口。业务方无需关心底层对接的是哪家对象存储（MinIO、阿里云 OSS、腾讯云 COS 等），只需在调用时指定**存储源名称**即可。

## 目录

- [文件上传](./upload.md)
- [分页查询图库](./list.md)
- [删除图库记录](./delete.md)
- [校验图库 id 是否合法](./validate.md)
- [批量根据 id 查询 url](./get-urls.md)

## 通用约定

### Base URL

```
http://<服务地址>:8080/api/v1
```

### 请求

- 除上传接口为 `multipart/form-data` 外，其余接口均为 `application/json`（如有）。
- 每个响应都会携带 `X-Request-ID` 响应头，排查问题时请提供该 ID。

### 响应

统一响应结构如下，`code` 为 0 表示成功，非 0 为业务错误码（与 HTTP 状态码解耦）：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

常见业务错误码：

| code | 含义 |
| --- | --- |
| 0 | 成功 |
| 40000 | 请求参数错误 |
| 40400 | 资源不存在（如存储源、图库记录） |
| 50000 | 服务端内部错误 |

HTTP 状态码遵循标准语义：200 成功、400 参数错误、404 资源不存在、500 服务端错误。

## 缓存说明

图库相关查询采用 Redis 旁路缓存（cache-aside）：读接口优先查缓存，未命中回查 MySQL 并回填；上传落库、删除记录后失效对应缓存。缓存过期时间由配置 `redis.gallery-ttl` 控制（默认 10m）。Redis 故障时自动降级为直查数据库，不影响接口可用性。
